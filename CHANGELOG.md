# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

<!-- Entries generated by scripts/changelog-gen.py -->

## [1.3.0] - 2026-03-19

### Added

**Security:**
- `injection-defender.sh` — PostToolUse hook scans Read/Bash/WebFetch/WebSearch outputs for 50+ prompt injection patterns across 5 categories (instruction override, DAN/role-play, context manipulation, encoding/obfuscation, instruction smuggling). Exit 2 = block. Toggleable via `automation.injection_defense`.

**5 new agents (22 total):**
- `compliance-officer` — RGPD/GDPR, SOC2, PCI-DSS, HIPAA compliance audits with BLOCKER/WARNING/OK output
- `ai-engineer` — LLM APIs, RAG pipelines, embeddings, vector DBs, model serving, cost optimization
- `realtime-architect` — WebSockets, SSE, WebRTC, event sourcing, CQRS, presence systems
- `data-modeler` — Schema design, normalization, migrations, indexation, multi-tenancy, temporal data
- `schema-designer` — OpenAPI, GraphQL, Protobuf, contract-first, versioning, backwards compat

**Commands system (7 commands in `.claude/commands/`):**
- `/generate-adr` — Architecture Decision Record generator (C4 model), auto-numbering
- `/check-security` — Quick security audit (Bandit, CVE, secrets scan, OWASP patterns)
- `/new-module` — Scaffold module adapted to detected stack (FastAPI, Express, Django, etc.)
- `/check-backend` — Backend health check (endpoints, DB, queues, logs, migrations)
- `/incident-response` — Triage → diagnose → fix → post-mortem playbook
- `/new-entity` — Generate data entity (model, migration, CRUD endpoints, tests, seed data)
- `/deploy-checklist` — Pre-deploy verification checklist

**3 new skills (8 total):**
- `auto-research` — Self-improving skill eval loop (AutoResearch pattern): run skill against PASS/FAIL criteria, analyze failures, propose improvement, re-test
- `memory-sync` — Promote stable patterns from `learning.md` into manifest `custom_rules` when seen 3+ times
- `configure` — Natural language config: "désactive les notifs" → patch manifest + auto-regen

**Automation:**
- `manifest-regen.sh` — PostToolUse hook: auto-runs `gen.py` when `project.manifest.json` is edited
- Auto-regen at startup: `session-start.sh` detects manifest changes via hash, runs `gen.py --quiet` silently
- Dependency check at startup: warns if `terminal-notifier` missing on macOS
- `gen.py --quiet` — silent mode for automatic hooks
- Automation toggle flags: `notifications`, `subagent_logging`, `observability`, `injection_defense` (all default `true`)
- Monoagent/multiagent toggle (`automation.agent_mode: mono|team`)
- Cost tracking signal in session-start (token usage estimate from last sessions)

**1 new workflow (28 total):**
- `mcp-vetting` — Safely evaluate and approve new MCP servers: research → security review → sandbox test → decision gate → integration

### Fixed

- Notification click opened Script Editor on macOS — now uses `terminal-notifier` when available (`brew install terminal-notifier`)
- `.template/version.json` version stuck at 1.1.0 — updated to track actual releases
- CI routing check only read `CLAUDE.md`, not `@import`ed `workflows-table.md` — now reads both
- Makefile out of sync with CI — added `CONTRAT DE SORTIE` + routing table coverage checks locally
- `scripts/check.sh` routing coverage same fix as CI

## [1.2.0] - 2026-03-19

### Added

**5 new skills (auto-invocable via YAML frontmatter):**
- `code-review` — 9 specialized agents in parallel (test runner, linter, security, quality, performance, dependency, test quality, simplification, code reviewer) → verdict Ready/Needs Attention/Needs Work
- `writer-reviewer` — bias-free review pattern: implement in current session, review in fresh context
- `two-correction` — stops correction loops after 2 identical mistakes, restarts with improved prompt
- `epic-start` — scaffolds a GitHub Issue as isolated git worktree with TASK.md
- `epic-merge` — pre-merge validation, PR creation, worktree cleanup

**2 new workflows:**
- `riper.md` — Research → Innovate → Plan → Execute → Review methodology with explicit gates
- *(4 workflows from v1.1.0 promoted: `multi-agent-worktrees`, `context-handoff`, `notebook-review`, `cost-dashboard`)*

**1 new agent:**
- `memory-curator` — consolidates learning.md, deduplicates entries, promotes patterns to skills

**`gen.py` additions:**
- `.claudeignore` generated per stack (Python, JS/TS, Go, Rust, Java)
- Nested `CLAUDE.md` stubs for monorepos (`packages/`, `apps/`, `services/`)
- `Notification` hook — desktop + audio notification when Claude needs attention
- `SubagentStop` hook — logs subagent completion to `.template/agent-events.jsonl`
- `observability.sh` — PostToolUse event log for audit trail
- `--target cursor` — generates `.cursorrules` from manifest
- Model routing (`model_routing{}` in manifest — sonnet/opus/haiku per task type)
- Security deny rules (`BASE_DENY_PATTERNS` in settings.local.json)
- `AGENTS.md` copy of CLAUDE.md for Cursor/Codex/Amp/Jules compatibility

**`install.sh`:** full audit before copy — detects existing settings, MCP servers, custom hooks/agents/workflows; never silently overwrites; `cp -n` on agents and workflows; CLAUDE.md backed up if different

**Hooks improvements:**
- `pre-compact.sh` — saves session context to `.template/session-backup.md` before compaction
- `post-edit.sh` — radon cyclomatic complexity (Python), prompt injection scan, `.env` file block; now Edit/Write only (not Read)
- `stop.sh` — session summary file, `usage.jsonl` log, OS + audio notification, `auto-learn --deduplicate`
- `session-start.sh` — docs auto-detect (docs/, spec/, adr/), configurable injection (`context.docs_paths[]`), devcontainer/SSH detection, auto-start setup interview when manifest empty

**`auto-learn.py`:** confidence scoring (0.5→0.75→0.95), `--record-pattern` / `--evolve` flags, skill promotion to `.claude/skills/`

**`self-improve.py`:** session count heuristic fixed (unique days instead of entries/3)

**`CLAUDE.md`:** modular refactor with `@import` (agents-table, workflows-table, security-layers), Plan Mode guidance, context management rules (compact at 70%, two-correction rule, /batch /loop)

**`project.manifest.json`:** `claude_native_integrations[]`, `model_routing{}`, `context.compact_focus`, full agent/workflow lists

### Fixed

- `pre-bash-guard.sh` f-string bug: pattern name now correctly interpolated via `sys.argv[1]`
- `stop.sh` duplicate self-improve call (invalid `--type`/`--note` args) removed
- `post-edit.sh` false positives on Read operations and claudekit source files
- `.template/version.json` missing `source` field
- `install.sh` KNOWN_HOOKS and CLAUDEKIT_WORKFLOWS lists were outdated (missing 4 hooks, 8 workflows)

## [1.1.0] - 2026-03-18

### Added

**2 new agents (16 total):**
- `cost-analyst` — cloud cost optimization (AWS/GCP/Azure), LLM token reduction, rightsizing, reserved instances, budget alerts
- `spec-reader` — reads any spec document (PDF, MD, Notion, email) → manifest + backlog + GitHub issues

**10 new workflows (22 total):**
- `a-b-test.md` — power analysis, feature flag rollout, statistical significance, ship/kill/continue gate
- `data-quality.md` — Great Expectations, dbt tests, ISO 8000 quality score, SLA monitoring
- `llm-eval.md` — RAGAS, hallucination detection, BLEU/ROUGE, human eval (Cohen's κ), deploy gate
- `spec-to-project.md` — cahier des charges → manifest + learning.md + backlog + arch + GitHub issues
- `code-review.md` — structured PR review (BLOCKER/WARNING/SUGGESTION) → `gh pr review`
- `monitoring-setup.md` — Prometheus/Grafana/Loki/Jaeger/Sentry → dashboards + SLOs + alerts
- `cost-optimization.md` — cloud + LLM audit → recommendations by ROI + budget alerts + FinOps tagging
- `dependency-audit.md` — CVE scan + license check + phantom deps → report without modifying

**`gen.py`:** `--dry-run`, `--diff`, Pulumi, Deno 2.x, Nx affected, 8 new intents (23 total), auto-installs `pre-push` git hook

**`scripts/claudekit.py`:** unified CLI — validate, check, gen, bump, status, install

**`scripts/migrate-template.py`:** automatic manifest migration between versions (1.0.x → 1.1.0 → ...)

**`auto-learn.py`:** `--deduplicate` flag for cross-session deduplication

**`.claude/hooks/pre-push.sh`:** git pre-push hook — secret scan + lint + tests before every push

**`examples/`:** 2 new pre-filled manifests — `iac.manifest.json` (Terraform+AWS+EKS), `cli.manifest.json` (Python+Typer+PyPI)

## [1.0.0] - 2026-03-18

### Added

- 14 specialized agents (architect, reviewer, tester, deployer, explorer, security-auditor, debug-detective, doc-writer, performance-analyst, release-manager, data-engineer, ml-engineer, devops-engineer, template-improver) — each with HANDOFF JSON output contract + project-type checklists
- 14 end-to-end workflows (feature, bugfix, hotfix, release, security-audit, dependency-update, refactor, onboard, self-improve, db-migration, incident-response, performance-baseline, publish-package, api-design)
- 5 hooks: `session-start` (40+ stack markers, 7 operational signals), `user-prompt-submit` (15 intents, 8 injection patterns), `pre-bash-guard`, `post-edit`, `stop`
- `scripts/gen.py` — manifest-to-config generator (31 languages, 8 package managers, monorepos, serverless, data/ML, desktop)
- `scripts/auto-learn.py`, `self-improve.py`, `version-bump.py`, `changelog-gen.py`
- `install.sh` — one-liner installer (`curl -fsSL .../install.sh | bash`)
- 4 pre-filled example manifests (web-app, api, ml, mobile)
- `project.manifest.json` — single source of truth for stack, workflow, guards, agents, MCP servers
- CI: `validate.yml` (syntax + JSON + HANDOFF coverage + gen.py end-to-end), `release.yml` (auto GitHub Release + tag)
- Branch protection on main (CI required, 1 review, no force push)
